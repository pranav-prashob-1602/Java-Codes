package com.pranav.threads;

public class Table {
	
	//When more than one threads are accessing a synchronized block/method, they should acquire the same object lock.
	public synchronized void display(int a) {
		for(int i=1;i<11;i++) {
			System.out.println(i*a);
		}
	}
	
}
